# Warera API key (issued by the game; needed for the 500 req/min tier).
WARERA_API_KEY=

# Upstash Redis REST credentials (from the Upstash console).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
