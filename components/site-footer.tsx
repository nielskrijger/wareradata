export function SiteFooter() {
  return (
    <footer className="text-muted-foreground mt-auto border-t px-6 py-4 text-center text-xs sm:px-8 lg:px-12">
      Data is updated once per hour (if the 🐹 is alive).
    </footer>
  )
}
